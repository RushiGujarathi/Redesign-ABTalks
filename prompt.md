# Project Notes

- Fixed the `Submit Work` button in `src/pages/DayChallenge.tsx` so it belongs to the form and is disabled until both GitHub and LinkedIn URLs are entered.
- Added bottom padding and spacing in `DayChallenge.tsx` to prevent the sticky submit button from overlapping the LinkedIn input.
- Updated `README.md` with correct local run instructions:
  - `npm install`
  - `npm run dev`
  - Visit `http://localhost:5173`
  - Optional: `npm run build`, `npm run preview`
- Moved the mobile/PC view toggle in `src/components/Layout.tsx` from the left side to the right side.
- Adjusted the toggle position slightly upward and inward for better visibility.
